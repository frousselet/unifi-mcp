# Required: Your UniFi Site Manager API key
# Generate at https://unifi.ui.com (API section)
UNIFI_API_KEY=your-api-key-here

# Optional: Override API base URL (default: https://api.ui.com/v1)
# UNIFI_API_BASE_URL=https://api.ui.com/v1

# Optional: HTTP timeout in seconds (default: 30)
# UNIFI_API_TIMEOUT=30

# --- UniFi Network API (local console) ---
# Optional: IP or hostname of your UniFi console (e.g. 192.168.1.1)
# UNIFI_NETWORK_HOST=192.168.1.1

# Optional: Separate API key for the Network API (falls back to UNIFI_API_KEY)
# Generate in UniFi Network > Settings > Control Plane > Integrations
# UNIFI_NETWORK_API_KEY=your-network-api-key-here

# Optional: Verify SSL certificate (default: false, consoles use self-signed certs)
# UNIFI_NETWORK_VERIFY_SSL=false
